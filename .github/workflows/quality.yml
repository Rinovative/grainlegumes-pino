name: Quality Checks

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  quality:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Micromamba
        uses: mamba-org/setup-micromamba@v1
        with:
          environment-file: environment-dev.yml
          cache-environment: true
          cache-downloads: true
          init-shell: bash

      - name: Run Ruff (Lint + Format)
        shell: bash -l {0}
        run: |
          ruff check .
          ruff format --check .

      - name: Run Type Checking (mypy)
        shell: bash -l {0}
        run: |
          mypy .

      - name: Notebook QA
        shell: bash -l {0}
        run: |
          nbqa ruff model_training/notebooks/
          nbqa mypy model_training/notebooks/

      - name: Complexity Analysis
        shell: bash -l {0}
        run: |
          radon cc -s -a .
          complexipy --paths . || true
